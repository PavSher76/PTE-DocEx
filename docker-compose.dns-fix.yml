# Public DNS — only for home/VPS, NOT behind corporate proxy (breaks internal hostnames).
# Usage: docker compose -f docker-compose.yml -f docker-compose.dns-fix.yml up --build
services:
  backend:
    dns:
      - 8.8.8.8
      - 1.1.1.1
  frontend:
    dns:
      - 8.8.8.8
      - 1.1.1.1
  languagetool:
    dns:
      - 8.8.8.8
      - 1.1.1.1
